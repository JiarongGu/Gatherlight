// Atoms — the in-house surface over antd primitives. Molecules/organisms/screens
// import these from `@/ui/atoms`, never `antd` directly, so the underlying UI kit
// is swappable and the dependency is centralized here.
export {
  Button,
  Input,
  Tag,
  Alert,
  Tooltip,
  Modal,
  Drawer,
  Switch,
  Spin,
  Dropdown,
  Menu,
  Image,
  Empty,
  Collapse,
  Space,
  App,
  Card,
  Typography,
  type MenuProps
} from 'antd';
