{
  "name": "@daily-planner/processors",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Workflow-step library for the chat console: spawns the local claude CLI, tracks edits, runs the two-gate git flow, and validates 智库 (.claude/) changes.",
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.3"
  }
}
